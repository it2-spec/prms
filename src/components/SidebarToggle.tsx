"use client";

export default function SidebarToggle() {
  // The event listeners for toggling the sidebar and mobile back are already
  // handled by public/cuba/js/sidebar-menu.js. Adding them here creates
  // duplicate listeners that toggle classes twice, neutralizing the action.
  return null;
}

